# ===== Repository A (Original Repo) =====
REPO_A_PATH=/root/opencti
REPO_A_URL=https://github.com/raysunplus/opencti.git
REPO_A_BRANCH=main

# ===== Repository B (Your fork / customized repo) =====
REPO_B_PATH=/root/opencti-custom
REPO_B_URL=https://github.com/fdahmadi/opencti-custom.git
REPO_B_BRANCH=main

# ===== GitHub Credentials (Required for PR creation) =====
GITHUB_TOKEN=
GITHUB_USER=fdahmadi
GITHUB_OWNER=fdahmadi
GITHUB_REPO=opencti-custom

# ===== Claude (Optional - for intelligent PR creation) =====
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# ===== File Sync Options =====
# Dry run mode (true/false) - If true, PRs will be previewed but not created
DRY_RUN=false

# Include patterns - Comma-separated list of patterns to include only matching files
# Example: src/,docs/ - Only create PRs for files in src/ and docs/ directories
# Leave empty to include all files
INCLUDE_PATTERNS=

# Exclude patterns - Comma-separated list of patterns to exclude from PRs
# Example: node_modules/,*.log,config/ - Exclude node_modules directory, log files, and config directory
# Leave empty to not exclude any files
EXCLUDE_PATTERNS=

# Conflict resolution strategy when files exist in both repositories with different content
# Options:
#   overwrite - Overwrite with content from Repository A (default)
#   keep - Keep existing content in Repository B
#   backup - Create a backup of existing content and then overwrite
#   merge - Merge content from both repositories (simple merge)
CONFLICT_STRATEGY=overwrite

# ===== Cron Schedule (Optional) =====
# Default: 0 * * * * (every hour)
# Examples:
#   */5 * * * *   - Every 5 minutes
#   */10 * * * *  - Every 10 minutes
#   0 * * * *     - Every hour
#   0 */2 * * *   - Every 2 hours
# CRON_SCHEDULE=0 * * * *

# ===== Commit Processing Delay (Optional) =====
# Wait time between processing each commit (in minutes)
# Default: 3 minutes
# COMMIT_WAIT_TIME=3

# ===== Rate Limit Protection (Optional) =====
# Maximum tokens per request (default: 180000, safe limit below 200k API hard limit)
# MAX_TOKENS_PER_REQUEST=180000

# Maximum file size in characters before truncation (default: 30000 = 30KB)
# MAX_FILE_SIZE=30000

# Maximum number of files to process per request (default: 10)
# If more files are changed, only the most important ones will be processed
# MAX_FILES_TO_PROCESS=10

# Maximum diff size in characters before truncation (default: 500000 = 500KB)
# MAX_DIFF_SIZE=500000

# Retry configuration for rate limit errors
# RETRY_MAX_ATTEMPTS=3
# RETRY_BASE_DELAY_MS=60000

